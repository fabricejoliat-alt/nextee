export default function AppLoading() {
  // Route-level pages provide their own contextual loading feedback.
  // Keep the root transition silent to avoid a full-screen loader after login.
  return null;
}
