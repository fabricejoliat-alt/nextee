export default function PlayerActivitiesRouteLoading() {
  // The Player shell already provides a stable background while the route hydrates.
  // Avoid adding the global centered spinner on top of it.
  return null;
}
