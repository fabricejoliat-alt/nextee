export default function PlayerRouteLoading() {
  // Player pages render their own section-level loading states.
  // Keeping this boundary empty avoids the global centered spinner on refresh.
  return null;
}
